[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Disable Neon/DB plugin — we use Netlify Blobs, don't need Postgres
[[plugins]]
  package = "neon-buildhooks"
  [plugins.inputs]
    disabled = true
